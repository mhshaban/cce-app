# خارطة طريق CCE StableOS

تُراجع هذه الخارطة عند كل إصدار. الأولويات تعبّر عن ترتيب التنفيذ، ولا تمثل مواعيد تسليم ثابتة.

## المرحلة الحالية — تثبيت الأساس (P0)

- [x] هندسة موحدة لترويسات Desktop وTablet وMobile.
- [x] توحيد Safe Area على الجوال مع الصفحة الرئيسية.
- [x] فصل الحالة التشغيلية للخيل عن الحالة الصحية في الواجهة.
- [x] مزامنة إكمال المهام الصحية مع ملخص الخيل والتنبيهات.
- [x] إصلاح تحميل التنبيهات حسب الصلاحيات والبوابات.
- [x] حماية عرض طلبات الحجز وإصلاح خطط أقساط المصروفات والنشاط المالي الأخير في v4.6.5.
- [x] إنشاء الوثائق الرسمية للمشروع.
- [x] تطبيق ترحيل قاعدة البيانات v4.6.4 على Supabase والتحقق من الدوال والمشغلات والملخصات.
- [x] تجميع مخطط الأساس وسلسلة ترحيلات Supabase داخل المستودع واختبار بنائها من الصفر.
- [x] إضافة CI لفحص JavaScript واختبارات الانحدار وبناء سلسلة قاعدة البيانات.
- [x] نقل الحجز العام إلى RPC ذرية بسعر وسعة وقفل زمني من الخادم.
- [x] جعل حسابات Auth الجديدة غير نشطة وبدون دور افتراضيًا.
- [x] فصل طلب الحجز وبيانات السلامة المحمية عن سجل المالية.
- [x] فصل حالة الحجز عن حالة الدفع وإضافة انتقالات حالة محكومة.
- [x] إعادة احتساب ملخصات صحة الخيل بعد الإضافة والتعديل والحذف.
- [x] تضييق RLS للأحداث الصحية حسب Medical وVaccination وCare وتصحيح توجيه Trainer Portal.
- [x] بناء واختبار تقسيم دفعات التدريب 50/50 وربط الدفعة والحصة بمدرب ثابت.
- [x] إضافة حد قطع v4.8.1 يحفظ مبالغ Lesson التاريخية ويمنع تقسيمها بأثر رجعي.
- [ ] اختبار قبول يدوي على iPhone/Android/Tablet/Desktop بأدوار مختلفة.
- [x] تطبيق ترحيل v4.7.0 على الإنتاج وتشغيل ملف التحقق.
- [x] إيقاف التسجيل العام يدويًا من إعدادات Supabase Auth والتحقق منه.
- [x] تطبيق تصحيح v4.8.1 وتشغيل ملف التحقق بأربعة أصفار مع حفظ إجمالي المدفوع.
- [ ] نشر واجهة v4.8.1، ثم تفعيل الباقة الجديدة المحددة فقط واختبار 35/35.

## المرحلة التالية — الاعتمادية والأمان (P0/P1)

- إكمال اختبار قبول مصفوفة RLS وRPC على Supabase الحقيقي لكل دور.
- إضافة اختبارات تكامل لمسارات تسجيل دفعة وجدول المدرب ضد بيئة تجريبية.
- إضافة عملية حذف/إخفاء تلقائي لبيانات سلامة الحجز بعد انتهاء مدة الاحتفاظ.
- إزالة الاعتماد التدريجي على الحقول الصحية القديمة بعد نقل جميع المستهلكين إلى الأحداث الموحدة.
- تفكيك `app-core.js` تدريجيًا إلى خدمات Bookings وFinance وNotifications دون تغيير السلوك.

## المرحلة الثالثة — الخدمات والتنبيهات (P1)

- خدمة تنبيهات خادمية موحدة تعتمد على أحداث النظام.
- Supabase Realtime للتحديثات الفورية داخل التطبيق.
- Web Push حقيقي عبر اشتراكات Push وخدمة خلفية، بدل الاعتماد على صفحة متصفح مفتوحة.
- مركز تفضيلات تنبيهات حسب المستخدم والدور ونوع الحدث.

## المرحلة الرابعة — المنصة المؤسسية (P1/P2)

- لوحات مؤشرات قابلة للتخصيص وتقارير تشغيلية ومالية متقدمة.
- دورة حياة كاملة للإيواء والعقود والفوترة.
- CRM موحد وتاريخ تواصل العميل.
- دعم فروع وإسطبلات متعددة عند اعتماد نموذج Multi-tenant.
- API وعقود تكامل موثقة للأنظمة الخارجية.

## بوابة الخروج لكل مرحلة

لا تُعد المرحلة مكتملة قبل نجاح الاختبارات، تحديث الوثائق وCHANGELOG، وجود خطة رجوع لقاعدة البيانات، ومراجعة الأمان والصلاحيات.
